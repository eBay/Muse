// A normal plugin project should not specify a different version
// of some dep which is already specified in some lib plugin.
// This function checks if there is any such case.

export function checkLibDeps() {
  // get all lib plugins
}
