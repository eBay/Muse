It means a request to do some modification on Muse registry.

There maybe multiple changing statuses for every request.

The change of the request is applied only when all statuses' results are `success`